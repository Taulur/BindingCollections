<Page x:Class="BindingCollections.Pages.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:BindingCollections.Pages"
       xmlns:validators="clr-namespace:BindingCollections.ValidationRules"
      mc:Ignorable="d" 
      d:DesignHeight="400" d:DesignWidth="800" 
      Title="LoginPage">

    <StackPanel Style="{DynamicResource StackBack}">
        <Label Style="{StaticResource MenuLabel}" Content="Вход пользователя"/>
        <Grid Style="{DynamicResource GridBack}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Label Grid.Row="0" Grid.Column="0" Content="id пользователя: "/>
            <Label Grid.Row="1" Grid.Column="0" Content="Пароль: "/>
            <StackPanel Grid.Row="0" Grid.Column="1">
                <TextBox x:Name="idBox"  >
                    <TextBox.Text>
                        <Binding Path="Id" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:MainValidation/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <ItemsControl Foreground="Red" FontSize="12" ItemsSource="{Binding ElementName=idBox, Path=(Validation.Errors)}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Center" Text="{Binding ErrorContent}" TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
            <StackPanel  Grid.Row="1" Grid.Column="1">
                <TextBox x:Name="passwordBox"  >
                    <TextBox.Text>
                        <Binding Path="Password" UpdateSourceTrigger="PropertyChanged">
                            <Binding.ValidationRules>
                                <validators:PasswordValidation/>
                            </Binding.ValidationRules>
                        </Binding>
                    </TextBox.Text>
                </TextBox>
                <ItemsControl Foreground="Red" FontSize="12" ItemsSource="{Binding ElementName=passwordBox, Path=(Validation.Errors)}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <TextBlock HorizontalAlignment="Center" Text="{Binding ErrorContent}" TextWrapping="Wrap"/>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>

        </Grid>
        <Button Click="Login"  Content="Войти"/>
        <Button Click="Register" Content="Регистрация"/>
    </StackPanel>
</Page>
